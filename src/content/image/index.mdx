export default function Index({ index=0, ...props }) {
  if (!props.frontmatter) return null;

return (

<div className={props.frontmatter.slug==='about'?'h-600px':'h-fit'} id={`${props.frontmatter.title.replace(' ', '_').toLowerCase()}`}>
    <Image featuredImage={props.frontmatter.featuredImage} />

    <ImageText
      title={props.frontmatter.title}
      description={props.frontmatter.description}
      sub={props.frontmatter.sub}
    />

</div>
); }
