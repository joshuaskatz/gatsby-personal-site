import Image from '../../components/image/Image.tsx'
import ImageText from '../../components/image/ImageText.tsx'

export default function Index({ index=0, className='', ...props }) {
  if (!props.frontmatter) return null;

return (

<div className={className} id={`${props.frontmatter.title.replace(' ', '_').toLowerCase()}`}>
    <Image featuredImage={props.frontmatter.featuredImage} />

    <ImageText
      title={props.frontmatter.title}
      description={props.frontmatter.description}
      sub={props.frontmatter.sub}
    />

</div>
); }
